task:
  name: math_smoke
  benchmark_id: math
  domain: math
  split: smoke
  smoke_subset: local_contract_v1
  dataset_path: data/benchmarks/smoke/math_smoke.jsonl
  metric_name: exact_match
  evaluator:
    type: exact_match
